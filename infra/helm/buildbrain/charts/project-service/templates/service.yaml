apiVersion: v1
kind: Service
metadata:
  name: project-service
spec:
  type: ClusterIP
  selector:
    app: project-service
  ports:
  - port: {{ .Values.port }}
    targetPort: {{ .Values.port }}
    name: http

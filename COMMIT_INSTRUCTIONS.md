Commit & Branch Instructions

I prepared core-scaffold files (Dockerfiles, k8s manifests, Helm chart) in the workspace.

To create a feature branch and commit your changes locally:

```bash
cd c:/Users/jclpublicdesktoptbs/Desktop/buildbrainos
git checkout -b feat/core-scaffold
git add .
git commit -m "feat(core): add multi-stage Dockerfiles, k8s manifests, and Helm chart skeleton"
git push -u origin feat/core-scaffold
```

If you'd like, I can prepare a patch file instead. Tell me which you prefer.
